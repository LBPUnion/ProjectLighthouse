@model (string Url, string Title)

<iframe src="@Model.Url"
        title="@Model.Title"
        style="width: 100%; height: 0; border: 0; overflow: hidden"
        scrolling="no"
        seamless="seamless">
</iframe>